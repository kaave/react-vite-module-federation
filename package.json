{
  "name": "react-vite",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "app-shell",
    "app-1"
  ],
  "scripts": {
    "build": "pnpm  --parallel --filter \"./**\" build",
    "serve": "pnpm  --parallel --filter \"./**\" preview",
    "build:remotes": "pnpm --parallel --filter \"./app-1\" build",
    "serve:remotes": "pnpm --parallel --filter \"./app-1\" serve",
    "dev:hosts": "pnpm --filter \"./app-shell\" dev",
    "stop": "kill-port --port 5000,5001"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.1.10",
    "kill-port": "^2.0.1"
  }
}
